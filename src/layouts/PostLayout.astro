---
import { type BlogSchema } from "../content.config";
import BaseLayout from "./BaseLayout.astro";
import dayjs from "dayjs";
import localizedFormat from "dayjs/plugin/localizedFormat";
export interface Props extends BlogSchema {}

const { title, description, pubDate, updatedDate, badge, heroImage, tags = [] } = Astro.props;
dayjs.extend(localizedFormat);
const displayDate = dayjs(pubDate).format("ll");
---

<BaseLayout title={title} description={description} ogType="article">
  <main>
    <article class="prose prose-img:mx-auto">
      <h1 class="title mt-5 text-3xl font-bold break-words mb-2 text-primary">
        {title}
      </h1>

      <div class="flex items-center gap-2">
        {pubDate && <time>{displayDate}</time>}
        <span>▪</span>
        <span class="text-primary/60 text-sm">{badge}</span>
      </div>

      <div class="border-l-4 border-info/60 pl-4 mt-4 mb-10">
        <div class="flex flex-wrap gap-2">
          {
            tags &&
              tags.map((tag) => (
                <a
                  href={`/blog/tag/${tag}`}
                  class="text-sm text-info/60 hover:text-info/80
                  transition-colors duration-150 font-normal underline-offset-2"
                >
                  {tag}
                </a>
              ))
          }
          {updatedDate && <span class="text-neutral text-sm">Updated {updatedDate}</span>}
        </div>
      </div>
      <div class="prose-table:overflow-x-auto prose-table:block">
        <slot />
      </div>
    </article>
  </main>
</BaseLayout>
