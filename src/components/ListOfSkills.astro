---
import Skills from "./icons/Skills.astro";

export const techSkills: string[] = [
  "TypeScript, Node.js, Express, SQL, Git",
  "Cierto conocimiento de Ruby y Python",
  "LaTex, BibTex, GeoGebra, Zotero",
  "Sonic Pi, MuseScore, Reaper",
];

const { indices = [] } = Astro.props;
const filteredSkills = indices
  .map((index) => techSkills[index])
  .filter((skill) => skill !== undefined);
---

<div class="mb-5 mt-[-1rem]">
  <div class="text-2xl w-full font-bold flex items-center">
    <Skills class="ml-0 mr-4" />Tecnologías
  </div>
</div>

<ul id="skill-list" class="list-disc columns-2 mx-6 mb-8">
  <div class="ml-3">
    {filteredSkills.map((skill) => <li>{skill}</li>)}
  </div>
</ul>
