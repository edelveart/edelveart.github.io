---
interface JobProps {
  title: string;
  subtitle: string;
  description: string;
  buttonText: string;
  moreInfo: string;
}
const { title, subtitle, description, moreInfo = "Ver más" }: JobProps = Astro.props as JobProps;
---

<div class="flex">
  <div class="education__time">
    <span class="w-3 h-3 bg-primary block rounded-full mt-1"></span>
    <span class="education__line bg-primary block h-full w-[2px] translate-x-[4.5px]"></span>
  </div>
  <div class="experience__data bd-grid px-5">
    <h3 class="font-semibold">{title}</h3>
    <span class="font-light text-sm">{subtitle}</span>

    {
      description && (
        <details class="mt-2" id="detailsElement">
          <summary class="text-sm cursor-pointer flex items-center gap-2">{moreInfo}</summary>

          <p class="my-2 text-pretty whitespace-pre-line max-w-[700px]">{description}</p>
        </details>
      )
    }
  </div>

  <style>
    summary {
      display: inline-flex;
      align-items: center;
    }

    summary::before {
      content: "";
      display: block;
      border-left: 5px solid transparent;
      border-right: 5px solid transparent;
      border-top: 8px solid #2bbdf8;

      transition: transform 0.3s ease-in-out;
    }

    details[open] summary::before {
      transform: rotate(180deg);
    }
  </style>
</div>
