---
import { allProjects } from "@components/ProjectsComponent.astro";
import { Image } from "astro:assets";
import { BORDER_OPACITY, GET_COLORS_METADATA } from "@styles/utilsCSS";

import MainFigurateNumImage from "@assets/main-figuratenum.webp";
import MainTonnetzImage from "@assets/main-tonnetz.webp";
import MainModularImage from "@assets/main-modular.webp";

const cardBase = `
  block relative w-full
  p-0
  ${BORDER_OPACITY}
  overflow-hidden
  aspect-[16/8]
  lg:aspect-auto
`;

const badge = `
  absolute top-0 left-0
  m-2 p-1
  bg-base-100
  rounded-sm
  ${GET_COLORS_METADATA("cardsImg")}
  text-xs
  z-5
`;
---

<!-- https://svgomg.net/ --><!-- https://vecta.io/nano -->
<section
  class="grid
    grid-cols-1
    lg:grid-cols-3
    gap-x-4
    gap-y-5
    lg:auto-rows-fr"
>
  <!-- 1 — FIGURATENUM / GRANDE -->
  <a
    href={allProjects[2].url}
    target="_blank"
    rel="noopener noreferrer"
    class={`${cardBase} lg:col-span-2 lg:row-span-2`}
  >
    <div class={badge}>
      <span>FIGURATENUM</span>
      <!-- <span>FigurateNum • PyPI</span> -->
    </div>

    <Image
      src={MainFigurateNumImage}
      class="w-full
        h-full
        object-cover
        object-center"
      width={800}
      height={520}
      format="webp"
      fetchpriority="high"
      loading="eager"
      alt="FigurateNumber - Phase Portrait"
    />
  </a>

  <!-- 2 — MODULAR FORMS -->
  <a
    href={allProjects[1].url}
    target="_blank"
    rel="noopener noreferrer"
    class={`${cardBase} lg:col-span-1`}
  >
    <div class={badge}>
      <span>MODULAR FORMS</span>
      <!-- <span>Modular Forms • RubyGems</span> -->
    </div>

    <Image
      src={MainModularImage}
      class="w-full
        h-full
        object-cover
        object-center
        invert
        hue-rotate-270"
      width={400}
      height={280}
      format="webp"
      loading="lazy"
      fetchpriority="low"
      alt="Math-Modular-Elliptic-Image"
    />
  </a>

  <!-- 3 — TS-TONNETZ -->
  <a
    href={allProjects[0].url}
    target="_blank"
    rel="noopener noreferrer"
    class={`${cardBase} lg:col-span-1`}
  >
    <div class={badge}>
      <span>TS-TONNETZ</span>
      <!-- <span>TS-Tonnetz • npm</span> -->
    </div>

    <Image
      src={MainTonnetzImage}
      class="w-full
        h-full
        object-cover
        object-center
        invert
        hue-rotate-180"
      width={400}
      height={280}
      format="webp"
      loading="lazy"
      fetchpriority="low"
      alt="ts-tonnetz simplices portrait"
    />
  </a>
</section>
