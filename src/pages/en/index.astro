---
import BaseLayout from "../../layouts/en/BaseLayout.astro";

import Biography from "../../components/en/Biography.astro";
import IndexLinks from "@components/IndexLinks.astro";

import ProjectsComponent, { allProjects } from "../../components/ProjectsComponent.astro";
import Experience, { allExperiences } from "../../components/en/Experience.astro";
import ListOfSkills from "@components/ListOfSkills.astro";
import { techSkillsEnglish } from "../../components/ListOfSkills.astro";
import Education from "../../components/en/Education.astro";
import Mordell from "@components/Mordell.astro";
---

<BaseLayout sideBarActiveItemID="home">
  <div class="pb-8"></div>
  <Mordell />
  <div class="pb-3 mt-5">
    <div class="text-3xl font-bold leading-snug py-0 flex">
      Hi, I'm Edgar
      <p class="ml-2 animate-bounce">👋</p>
    </div>
    <div class="py-1 font-bold text-primary">Software Developer and Math Enthusiast</div>

    <div class="py-2 mt-4">
      <Biography />
      <IndexLinks contactText="Contact me" />
    </div>

    <Experience workExperiences={allExperiences} indices={[0, 1]} />
    <ProjectsComponent headTitle={"Libraries"} projects={allProjects} indices={[0, 2, 5]} />
    <div class="m-16"></div>
    <ListOfSkills title="Technologies" techSkills={techSkillsEnglish} indices={[0, 1]} />
    <!-- <Education /> -->

    <!-- <div>
    <div class="text-3xl w-full font-bold mb-5 mt-10">Latest from blog</div>
    </div> -->
    <!-- {
      last_posts.map((post) => (
        <>
          <HorizontalCard
            title={post.data.title}
            img={post.data.heroImage}
            desc={post.data.description}
            url={"/blog/" + createSlug(post.data.title, post.slug)}
            target="_self"
            badge={post.data.badge}
          />
          <div class="divider my-0" />
        </>
      ))
    } -->
  </div>
</BaseLayout>
